spring:
  application:
    name: moreroom

  profiles:
    active: ${PROFILES_ACTIVE}  # prod or other profile names (replace it with ${PROFILES_ACTIVE} when committing & pushing)

  config:
    import: optional:file:.env[.properties]