import { css } from '@emotion/react';

export const filterContainer = css`
  width: 100%;
  display: flex;
  white-space: nowrap;
  column-gap: 0.5rem;
  padding: 1rem;
  overflow-x: scroll;
  box-sizing: border-box;
`;
