import { css } from '@emotion/react';

export const filterContainer = css`
  display: flex;
  align-items: center;
  column-gap: 1rem;
  margin-top: 1rem;
  white-space: nowrap;
  overflow-x: scroll;
`;
